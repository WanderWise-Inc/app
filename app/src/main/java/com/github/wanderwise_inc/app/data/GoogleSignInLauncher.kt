package com.github.wanderwise_inc.app.data

/*
import android.content.Intent
import android.util.Log
import androidx.activity.result.ActivityResultLauncher

interface GoogleSignInLauncher {
    fun launchSignIn()
}

class RealGoogleSignInLauncher(private val signInLauncher: ActivityResultLauncher<Intent>, private val signInIntent: Intent) : GoogleSignInLauncher {
    //private val providers = arrayListOf(AuthUI.IdpConfig.GoogleBuilder().build())
    // Create and launch sign-in intent
    override fun launchSignIn() {
        Log.d("TESTING SIGN IN BUTTON", "BEFORE LAUNCHING IN SIGN IN")
        signInLauncher.launch(signInIntent)
    }
}*/
